projection;
use draft;

define behavior for ZRAPH_C_TravelWDTP alias Travel
{
  use create;
  use update;
  use delete;

  use action acceptTravel;
  use action rejectTravel;

  use association _Booking { create; with draft; }
  use association _RoomReservation { create; with draft; }
}

define behavior for ZRAPH_C_BookingWDTP alias Booking
{
  use update;
  use delete;

  use association _Travel { with draft; }
  use association _BookingSupplement { create; with draft; }
}

define behavior for ZRAPH_C_BookingSupplementWDTP alias BookingSupplement
{
  use update;
  use delete;

  use association _Travel { with draft; }
}

define behavior for ZRAPH_C_RoomReservationWDTP alias RoomReservation
{
  use update;
  use delete;

  use association _Travel { with draft; }
}