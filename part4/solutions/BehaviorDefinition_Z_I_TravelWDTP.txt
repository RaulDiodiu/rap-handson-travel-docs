managed;
with draft;

define behavior for ZRAPH_I_TravelWDTP alias Travel
implementation in class zbp_raph_##_i_travelwdtp unique
persistent table /dmo/a_travel_d
draft table z##_d_travel
lock master
total etag LastChangedAt
etag master LocalLastChangedAt
{
  create;
  update;
  delete;

  association _Booking { create; with draft; }
  association _RoomReservation { create; with draft; }

  field ( numbering : managed, readonly ) TravelUUID;
  field ( readonly ) OverallStatus; 

  determination setInitialStatus on modify { create; }

  validation validateCustomer on save { create; field CustomerID; }

  action acceptTravel result [1] $self;


  draft determine action Prepare
  {
    validation validateCustomer;
  }

  mapping for /dmo/a_travel_d
  {
    TravelUUID = travel_uuid;
    TravelID = travel_id;
    AgencyID = agency_id;
    CustomerID = customer_id;
    BeginDate = begin_date;
    EndDate = end_date;
    BookingFee = booking_fee;
    TotalPrice = total_price;
    CurrencyCode = currency_code;
    Description = description;
    OverallStatus = overall_status;
    LocalCreatedBy = local_created_by;
    LocalCreatedAt = local_created_at;
    LocalLastChangedBy = local_last_changed_by;
    LastChangedAt = last_changed_at;
    LocalLastChangedAt = local_last_changed_at;
  }
}

define behavior for ZRAPH_I_BookingWDTP alias Booking
persistent table /dmo/a_booking_d
draft table z##_d_booking
lock dependent by _Travel
etag master LocalLastChangedAt
{

  association _Travel { with draft; }
  association _BookingSupplement { create; with draft; }

  update;
  delete;

  field ( numbering : managed, readonly ) BookingUUID;
  field ( readonly ) TravelUUID, BookingDate;

  mapping for /dmo/a_booking_d
  {
    BookingUUID = booking_uuid;
    TravelUUID = parent_uuid;
    BookingID = booking_id;
    BookingDate = booking_date;
    CustomerID = customer_id;
    CarrierID = carrier_id;
    ConnectionID = connection_id;
    FlightDate = flight_date;
    FlightPrice = flight_price;
    CurrencyCode = currency_code;
    LocalLastChangedAt = local_last_changed_at;
  }
}

define behavior for ZRAPH_I_BookingSupplementWDTP alias BookingSupplement
persistent table /dmo/a_bksuppl_d
draft table z##_d_bksup
lock dependent by _Travel
etag master LocalLastChangedAt
{

  association _Travel { with draft; }

  update;
  delete;

  field ( numbering : managed, readonly ) BookSupplUUID;
  field ( readonly ) TravelUUID, BookingUUID;


  mapping for /dmo/a_bksuppl_d
  {
    BookSupplUUID = booksuppl_uuid;
    TravelUUID = root_uuid;
    BookingUUID = parent_uuid;
    BookingSupplementID = booking_supplement_id;
    SupplementID = supplement_id;
    Price = price;
    CurrencyCode = currency_code;
    LocalLastChangedAt = local_last_changed_at;
  }
}

define behavior for ZRAPH_I_RoomReservationWDTP alias RoomReservation
persistent table zraph_##_roomrsv
draft table z##_d_roomrsv
lock dependent by _Travel
etag master LocalLastChangedAt
{

  association _Travel { with draft; }

  update;
  delete;

  field ( numbering : managed, readonly ) RoomResvnUUID;
  field ( readonly ) TravelUUID;

  mapping for zraph_##_roomrsv
  {
    RoomResvnUUID = room_rsv_uuid;
    TravelUUID = parent_uuid;
    RoomResvnID = room_rsv_id;
    HotelID = hotel_id;
    BeginDate = begin_date;
    EndDate = end_date;
    RoomType = room_type;
    RoomResvnPrice = room_rsv_price;
    CurrencyCode = currency_code;
    LocalLastChangedAt = local_last_changed_at;
  }
}